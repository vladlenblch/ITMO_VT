# 🎯 Лабораторная работа №4

## Основная лабораторная работа
SpringBoot + React + Redux + PostgreSQL + Rest API веб-приложение с авторизацией и проверки попадания точки в заранее заданную область на график с помощью формы или кликом по графику. Веб-приложение включает 2 страницы: начальаня страница авторизации и страница отправки формы.

## Дополнительное задание
Вместо хранения логина и хеша в одной таблице реализовать трёхкомпонентную модель: Principal - абстрактный идентификатор (user_id, UUID), Credential - способ подтверждения (пароль, TOTP, recovery code), Evidence - доказательство владения (например, HMAC(challenge, secret)). Позволить пользователю иметь несколько независимых учётных данных (например, пароль + 3 резервных кода). Удаление одного способа не блокирует вход. Вся логика через CDI-бин-стратегии (+ добавить валидацию через Zod).
